# LLM Configuration
# Choose your LLM provider: "ollama" (local) or "openai" (cloud)
LLM_PROVIDER=openai

# OpenAI Configuration (only needed if LLM_PROVIDER=openai)
# Optional: Custom base URL (leave empty for default api.openai.com)
# Examples: https://api.openai.com/v1 (default), Azure OpenAI endpoint, proxy, etc.
OPENAI_BASE_URL=https://ai-proxy.lab.epam.com
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini-2024-07-18

# Ollama Configuration (only needed if LLM_PROVIDER=ollama)
OLLAMA_MODEL=llama3.2:3b
# Other Ollama options: mistral:7b, phi3:mini, gemma2:2b

